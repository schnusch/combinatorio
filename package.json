{
  "name": "combinatorio",
  "version": "0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist \\\n&& tsc --emitDeclarationOnly --outFile dist/combinatorio.d.ts \\\n&& esbuild --bundle --sourcemap --platform=browser --target=es2019 --outfile=dist/combinatorio.js main.ts"
  },
  "devDependencies": {
    "esbuild": "*",
    "typescript": "*"
  }
}
